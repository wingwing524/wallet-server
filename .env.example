# Environment
NODE_ENV=development
PORT=5000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Railway Database Credentials (add your Railway PostgreSQL credentials here)
DATABASE_URL=postgresql://postgres:password@host:port/database

# Alternative individual credentials if needed
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=expense_tracker